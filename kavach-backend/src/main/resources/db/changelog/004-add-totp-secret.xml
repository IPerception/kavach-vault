<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- Stores the TOTP secret encrypted with the master key.
         NULL until the user completes TOTP setup in the wizard. -->
    <changeSet id="006-add-totp-secret" author="kavach">
        <sql>
            ALTER TABLE vault_user ADD COLUMN totp_secret_encrypted BLOB
        </sql>
    </changeSet>

</databaseChangeLog>
